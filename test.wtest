import mensajeros.*

test "El paquete no está pago"{
    assert.notThat(paquete.puedeSerEntregado(laMatrix,neo))
}

test "El paquete está pago, y lo entrega Jean a la Matrix"{
    paquete.estaPago(true)
    assert.that(paquete.puedeSerEntregado(laMatrix, jeanGray))
}

test "El paquete está pago pero no se puede llevar porque se supera el peso del puente"{
    paquete.estaPago(true)
    saraConnor.pesoPropio(70)
    saraConnor.vehiculo(camion)
    camion.acoplados(1)
    assert.notThat(paquete.puedeSerEntregado(puenteBrooklyn, saraConnor))
}

test "El paquete está pago y puede llevarse por Sara Connor en moto"{
    paquete.estaPago(true)
    saraConnor.pesoPropio(70)
    saraConnor.vehiculo(moto)
    assert.that(paquete.puedeSerEntregado(puenteBrooklyn, saraConnor))
}
